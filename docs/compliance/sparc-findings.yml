# SPARC finding dispositions — the register `bin/sparc_findings_to_hdf_amendments.rb`
# turns into HDF amendments.
#
# ── THE TWO DELIBERATE ABSENCES ARE RESOLVED (#1065, 2026-08-28) ──────────
#
# This block used to say: do NOT "fix" resolv and uri by adding entries here.
# That instruction has been carried out to its intended conclusion, and the
# reason it existed is now gone. Recorded rather than deleted, because the way
# it was closed is the precedent.
#
# Trivy reported four CRITICAL/HIGH findings against the shipping UBI9 image,
# all the same case — a shadowed Ruby default gem, where the vulnerable
# Ruby-shipped copy stays on disk while Bundler loads a patched one:
#
#   CVE-2026-27820  zlib    3.2.1 on disk  — was dispositioned below
#   CVE-2026-41316  erb     4.0.4 on disk  — was dispositioned below
#   CVE-2025-24294  resolv  0.6.0 on disk  — NO ENTRY, deliberately
#   CVE-2025-61594  uri     1.0.3 on disk  — NO ENTRY, deliberately
#
# The owner decision of 2026-08-24 was to record the honest residual rather than
# write a disposition to make the number look clean, and to hold #1065 open
# "until the residual is genuinely 0 — i.e. the shadowed copies are gone from
# the image, not dispositioned away."
#
# That is exactly what happened. **Ruby 3.4.4 -> 3.4.10** ships zlib 3.2.3,
# erb 4.0.4.1 (the upstream backport), resolv 0.7.1 and uri 1.0.4 — each at or
# above the version its advisory patches at. The vulnerable code left the image.
# Measured on a clean CI runner:
#
#   main@daf41392   4 requirement(s): failed=2 notApplicable=2 (amended)
#   PR #1078        1 requirement(s): failed=0        <- canary only
#
# `docs/compliance/thresholds/trivy-container.yml` is ratcheted to `high: 0`
# accordingly. NO disposition was added for resolv or uri, then or now.
#
# THE ZLIB AND ERB ENTRIES ARE NOW RETIRED (2026-08-29), along with the
# sqlite-libs one. An earlier revision of this header kept them on the grounds
# that the SBOM and grype paths key on different ids, so an inert entry was safer
# than retiring one that still matched. That reasoning was superseded by
# evidence: all three findings are absent from the shipping image, so they are
# `remediated` and belong in sparc-findings.retired.yml, per the owner decision
# of 2026-08-20 ("a remediated finding is RETIRED, not reviewed").
#
#   erb    — image ships 4.0.4.1, the backport the entry's own fixed_version named
#   zlib   — image ships 3.2.3, meeting the entry's fixed_version of >= 3.2.3
#   sqlite — image ships 3.34.1-11.el9_8, NOT the 3.34.1-9.el9_7 recorded; Red Hat
#            marks RHEL 9 "Not affected"; the scanner reports the CVE at no severity
#
# Each carries `verified_absent_on` and `verified_by` in the retired file. The gem
# evidence is direct image inspection plus CI's 4 -> 0 container measurement, NOT a
# local trivy gemspec scan — that path is unreliable per #1080.
#
# ONE RESIDUAL REMAINS, and it is not hidden: `json`. Ruby 3.4.10 still ships
# json 2.9.1 on disk (CVE-2026-54696, fixed upstream in 2.19.9). It carries no
# entry here for the same reason resolv and uri never did — it is a real
# finding, and the honest thing is to leave it visible. It does not reach the
# CRITICAL/HIGH gate because it is CVSS 3.7 LOW. The Gemfile pins
# `json >= 2.19.9`, so the LOADED copy is patched (2.21.2); the on-disk copy
# waits on upstream Ruby.
#
findings:
- cve_id: CVE-2025-30258
  package: gnupg2
  installed_version: 2.3.3-5.el9_7
  fixed_version: none available upstream (not-fixed)
  severity: LOW
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''dirmngr 2.2.40-1.1+deb12u2'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: gnupg2
    2.3.3-5.el9_7 [rpm]. Rewritten to describe what the image actually contains; discovery_date is the
    date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8).
    Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the
    image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    SEVERITY CORRECTED MEDIUM -> LOW: MEDIUM was the grading of the DEBIAN artifact this entry used to
    describe, which is not in the image. LOW is how grype 0.114.0 grades the artifact that IS present.
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: dirmngr 2.2.40-1.1+deb12u2 (removed with the Debian base image)
- cve_id: CVE-2025-68972
  package: gnupg2
  installed_version: 2.3.3-5.el9_7
  fixed_version: none available upstream (not-fixed)
  severity: MEDIUM
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''dirmngr 2.2.40-1.1+deb12u2'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: gnupg2
    2.3.3-5.el9_7 [rpm]. Rewritten to describe what the image actually contains; discovery_date is the
    date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8).
    Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the
    image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: dirmngr 2.2.40-1.1+deb12u2 (removed with the Debian base image)
- cve_id: CVE-2023-50495
  package: ncurses-base/ncurses-libs
  installed_version: 6.2-12.20210508.el9
  fixed_version: none available upstream (not-fixed)
  severity: LOW
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''libncursesw6 6.4-4'' and was marked remediated because that package was removed — true, and the
    Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without re-verification,
    and the CVE is present in the shipping image against a DIFFERENT artifact: ncurses-base 6.2-12.20210508.el9
    [rpm]; ncurses-libs 6.2-12.20210508.el9 [rpm]. Rewritten to describe what the image actually contains;
    discovery_date is the date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest,
    RHEL 9.8). Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING:
    the image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    SEVERITY CORRECTED MEDIUM -> LOW: MEDIUM was the grading of the DEBIAN artifact this entry used to
    describe, which is not in the image. LOW is how grype 0.114.0 grades the artifact that IS present.
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: libncursesw6 6.4-4 (removed with the Debian base image)
- cve_id: CVE-2026-4105
  package: systemd-libs
  installed_version: 252-67.el9_8.6
  fixed_version: none available upstream (not-fixed)
  severity: MEDIUM
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''libsystemd0 252.39-1~deb12u1'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: systemd-libs
    252-67.el9_8.4 [rpm]. Rewritten to describe what the image actually contains; discovery_date is the
    date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8).
    Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the
    image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: libsystemd0 252.39-1~deb12u1 (removed with the Debian base image)
- cve_id: CVE-2026-27171
  package: zlib
  installed_version: 1.2.11-40.el9
  fixed_version: none available upstream (not-fixed)
  severity: LOW
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''zlib1g 1:1.2.13.dfsg-1'' and was marked remediated because that package was removed — true, and
    the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: zlib 1.2.11-40.el9
    [rpm]. Rewritten to describe what the image actually contains; discovery_date is the date THIS artifact''s
    exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8). Fix: none available upstream
    (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the image is not released until
    this is fixed or documented as having no upstream fix (owner, 2026-08-20). SEVERITY CORRECTED MEDIUM
    -> LOW: MEDIUM was the grading of the DEBIAN artifact this entry used to describe, which is not in
    the image. LOW is how grype 0.114.0 grades the artifact that IS present. RE-REVIEWED 2026-09-04 (owner):
    accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest
    (vulnerability DB built 2026-09-03) re-confirmed the finding is still PRESENT in the shipping image,
    still fix-state not-fixed with no upstream fixed version, and unchanged in severity — so the deferral
    basis is factually unchanged and there is no remediation available to apply. Scanned the IMAGE, not
    an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further 28 days, to 2026-10-19. Evidence:
    grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the #1144/#1164 branch (vulnerability
    DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still PRESENT in the shipping image, still
    fix-state not-fixed with no upstream fixed version offered, and unchanged in severity - so the deferral
    basis is factually unchanged and there is no remediation available to apply. Scanned the IMAGE, not
    an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: zlib1g 1:1.2.13.dfsg-1 (removed with the Debian base image)
- cve_id: CVE-2022-3219
  package: gnupg2
  installed_version: 2.3.3-5.el9_7
  fixed_version: none available upstream (not-fixed)
  severity: LOW
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''dirmngr 2.2.40-1.1+deb12u2'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: gnupg2
    2.3.3-5.el9_7 [rpm]. Rewritten to describe what the image actually contains; discovery_date is the
    date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8).
    Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the
    image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: dirmngr 2.2.40-1.1+deb12u2 (removed with the Debian base image)
- cve_id: CVE-2022-27943
  package: libgcc/libstdc++
  installed_version: 11.5.0-14.el9
  fixed_version: none available upstream (not-fixed)
  severity: LOW
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''gcc-12-base 12.2.0-14+deb12u1'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: libgcc
    11.5.0-14.el9 [rpm]; libstdc++ 11.5.0-14.el9 [rpm]. Rewritten to describe what the image actually
    contains; discovery_date is the date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest,
    RHEL 9.8). Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING:
    the image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: gcc-12-base 12.2.0-14+deb12u1 (removed with the Debian base image)
- cve_id: CVE-2026-22185
  package: openldap
  installed_version: 2.6.8-4.el9
  fixed_version: none available upstream (not-fixed)
  severity: MEDIUM
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''libldap-2.5-0 2.5.13+dfsg-5'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: openldap
    2.6.8-4.el9 [rpm]. Rewritten to describe what the image actually contains; discovery_date is the date
    THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8). Fix:
    none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the image
    is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20). SEVERITY
    CORRECTED LOW -> MEDIUM: LOW was the grading of the DEBIAN artifact this entry used to describe, which
    is not in the image. MEDIUM is how grype 0.114.0 grades the artifact that IS present. RE-REVIEWED
    2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0 image scan
    of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is still PRESENT
    in the shipping image, still fix-state not-fixed with no upstream fixed version, and unchanged in
    severity — so the deferral basis is factually unchanged and there is no remediation available to apply.
    Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further 28 days,
    to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the #1144/#1164
    branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still PRESENT in
    the shipping image, still fix-state not-fixed with no upstream fixed version offered, and unchanged
    in severity - so the deferral basis is factually unchanged and there is no remediation available to
    apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: libldap-2.5-0 2.5.13+dfsg-5 (removed with the Debian base image)
- cve_id: CVE-2025-70873
  package: sqlite-libs
  installed_version: 3.34.1-11.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: LOW
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''libsqlite3-0 3.40.1-2+deb12u2'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: sqlite-libs
    3.34.1-10.el9_8 [rpm]. Rewritten to describe what the image actually contains; discovery_date is the
    date THIS artifact''s exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8).
    Fix: none available upstream (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the
    image is not released until this is fixed or documented as having no upstream fix (owner, 2026-08-20).
    RE-REVIEWED 2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0
    image scan of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is
    still PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version, and
    unchanged in severity — so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further
    28 days, to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the
    #1144/#1164 branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still
    PRESENT in the shipping image, still fix-state not-fixed with no upstream fixed version offered, and
    unchanged in severity - so the deferral basis is factually unchanged and there is no remediation available
    to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: libsqlite3-0 3.40.1-2+deb12u2 (removed with the Debian base image)
- cve_id: CVE-2026-27456
  package: libblkid/libmount/libsmartcols/libuuid
  installed_version: 2.37.4-25.el9
  fixed_version: none available upstream (not-fixed)
  severity: MEDIUM
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''bsdutils 1:2.38.1-5+deb12u3'' and was marked remediated because that package was removed — true,
    and the Debian artifact is gone. The claim was then carried across the Debian->UBI9 migration without
    re-verification, and the CVE is present in the shipping image against a DIFFERENT artifact: libblkid
    2.37.4-25.el9 [rpm]; libmount 2.37.4-25.el9 [rpm]; libsmartcols 2.37.4-25.el9 [rpm]. Rewritten to
    describe what the image actually contains; discovery_date is the date THIS artifact''s exposure was
    found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8). Fix: none available upstream (not-fixed).
    Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the image is not released until this is fixed
    or documented as having no upstream fix (owner, 2026-08-20). SEVERITY CORRECTED INFORMATIONAL -> MEDIUM:
    INFORMATIONAL was the grading of the DEBIAN artifact this entry used to describe, which is not in
    the image. MEDIUM is how grype 0.114.0 grades the artifact that IS present. RE-REVIEWED 2026-09-04
    (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest
    (vulnerability DB built 2026-09-03) re-confirmed the finding is still PRESENT in the shipping image,
    still fix-state not-fixed with no upstream fixed version, and unchanged in severity — so the deferral
    basis is factually unchanged and there is no remediation available to apply. Scanned the IMAGE, not
    an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further 28 days, to 2026-10-19. Evidence:
    grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the #1144/#1164 branch (vulnerability
    DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still PRESENT in the shipping image, still
    fix-state not-fixed with no upstream fixed version offered, and unchanged in severity - so the deferral
    basis is factually unchanged and there is no remediation available to apply. Scanned the IMAGE, not
    an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: bsdutils 1:2.38.1-5+deb12u3 (removed with the Debian base image)
- cve_id: CVE-2026-34743
  package: xz-libs
  installed_version: 5.2.5-8.el9_0
  fixed_version: none available upstream (not-fixed)
  severity: MEDIUM
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-08-20 (14 days). This entry previously described the DEBIAN package
    ''liblzma5 5.4.1-1'' and was marked remediated because that package was removed — true, and the Debian
    artifact is gone. The claim was then carried across the Debian->UBI9 migration without re-verification,
    and the CVE is present in the shipping image against a DIFFERENT artifact: xz-libs 5.2.5-8.el9_0 [rpm].
    Rewritten to describe what the image actually contains; discovery_date is the date THIS artifact''s
    exposure was found (grype 0.114.0 scan of sparc-ubi9-web:latest, RHEL 9.8). Fix: none available upstream
    (not-fixed). Tracked in risk-sentinel/sparc#1001. RELEASE-BLOCKING: the image is not released until
    this is fixed or documented as having no upstream fix (owner, 2026-08-20). SEVERITY CORRECTED INFORMATIONAL
    -> MEDIUM: INFORMATIONAL was the grading of the DEBIAN artifact this entry used to describe, which
    is not in the image. MEDIUM is how grype 0.114.0 grades the artifact that IS present. RE-REVIEWED
    2026-09-04 (owner): accepted for a further 21 days, to 2026-09-25. Evidence: grype 0.114.0 image scan
    of sparc-ubi9-web:latest (vulnerability DB built 2026-09-03) re-confirmed the finding is still PRESENT
    in the shipping image, still fix-state not-fixed with no upstream fixed version, and unchanged in
    severity — so the deferral basis is factually unchanged and there is no remediation available to apply.
    Scanned the IMAGE, not an SBOM (#862). RE-REVIEWED 2026-09-21 (owner): accepted for a further 28 days,
    to 2026-10-19. Evidence: grype 0.114.0 image scan of sparc-ubi9-web:latest, built from the #1144/#1164
    branch (vulnerability DB built 2026-09-21T06:39:24Z), re-confirmed the finding is still PRESENT in
    the shipping image, still fix-state not-fixed with no upstream fixed version offered, and unchanged
    in severity - so the deferral basis is factually unchanged and there is no remediation available to
    apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
  superseded_debian_finding: liblzma5 5.4.1-1 (removed with the Debian base image)
- cve_id: CVE-2026-53613
  package: libblkid/libmount/libsmartcols/libuuid
  installed_version: 2.37.4-25.el9
  fixed_version: none
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-08-20 (#1001). util-linux
    TOCTOU in mount(8): with an /etc/fstab entry carrying the `user`/`users` option, mount validates the
    target before the syscall, and a local unprivileged user with write access to an ancestor of the target
    can swap it to redirect the mount. MEASURED in the shipping image, not argued: `command -v mount`
    finds no mount(8), there is no /etc/fstab at all, and the runtime user is uid=1000(rails) non-root.
    Only the shared libraries from the util-linux source package are present, pulled in as dependencies;
    the vulnerable executable is not in the image. No fix is available upstream, so this would otherwise
    defer indefinitely against a program we do not ship.'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-08-20'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14662
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via integer wraparound in tsvector and tsquery functions This is executed
    by the PostgreSQL server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres`
    reports only postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package
    — and `command -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through
    the `pg` gem and libpq against a SEPARATE Postgres container, so the vulnerable program is not in
    this image at all — the same basis as CVE-2026-53613, where the util-linux libraries are present but
    mount(8) is not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14664
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via heap buffer overflow in regexp This is executed by the PostgreSQL server
    backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only postgresql-13.23-5.el9_8
    and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command -v` finds no postgres,
    postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem and libpq against a
    SEPARATE Postgres container, so the vulnerable program is not in this image at all — the same basis
    as CVE-2026-53613, where the util-linux libraries are present but mount(8) is not. No fix is available
    upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14668
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Information disclosure via type confusion in ctid selectivity estimator This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14669
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via long POSIX timezone abbreviation This is executed by the PostgreSQL server
    backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only postgresql-13.23-5.el9_8
    and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command -v` finds no postgres,
    postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem and libpq against a
    SEPARATE Postgres container, so the vulnerable program is not in this image at all — the same basis
    as CVE-2026-53613, where the util-linux libraries are present but mount(8) is not. No fix is available
    upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14670
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via plperl tied hash heap buffer overflow This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14671
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via type confusion in ''refint'' module This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14677
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). pltcl:
    plperl: PostgreSQL: Arbitrary code execution in 32-bit pltcl and plperl This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14679
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Stack buffer overflow via OUT parameter count manipulation This is executed by the PostgreSQL server
    backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only postgresql-13.23-5.el9_8
    and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command -v` finds no postgres,
    postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem and libpq against a
    SEPARATE Postgres container, so the vulnerable program is not in this image at all — the same basis
    as CVE-2026-53613, where the util-linux libraries are present but mount(8) is not. No fix is available
    upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-14680
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via type confusion with "internal" arguments This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-15741
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Privilege escalation via SQL injection in EXTRACT() deparse This is executed by the PostgreSQL server
    backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only postgresql-13.23-5.el9_8
    and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command -v` finds no postgres,
    postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem and libpq against a
    SEPARATE Postgres container, so the vulnerable program is not in this image at all — the same basis
    as CVE-2026-53613, where the util-linux libraries are present but mount(8) is not. No fix is available
    upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-15742
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL
    fuzzystrmatch: Arbitrary code execution via integer wraparound This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-16239
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via type confusion in cursor lifecycle This is executed by the PostgreSQL
    server backend. MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only
    postgresql-13.23-5.el9_8 and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command
    -v` finds no postgres, postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem
    and libpq against a SEPARATE Postgres container, so the vulnerable program is not in this image at
    all — the same basis as CVE-2026-53613, where the util-linux libraries are present but mount(8) is
    not. No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-6471
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). PostgreSQL:
    Arbitrary code execution via logical decoding plugin This is executed by the PostgreSQL server backend.
    MEASURED in the shipping image, not argued: `rpm -qa | grep -i postgres` reports only postgresql-13.23-5.el9_8
    and postgresql-private-libs-13.23-5.el9_8 — the CLIENT package — and `command -v` finds no postgres,
    postmaster, pg_ctl or initdb. SPARC reaches its database through the `pg` gem and libpq against a
    SEPARATE Postgres container, so the vulnerable program is not in this image at all — the same basis
    as CVE-2026-53613, where the util-linux libraries are present but mount(8) is not. No fix is available
    upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-74860
  package: libxml2
  installed_version: 2.9.13-14.el9_8.4
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: false_positive
  rationale: 'NOT APPLICABLE (vulnerable program absent) — first dispositioned 2026-09-21 (#1173). double-free/UAF
    in libxml2 Python bindings MEASURED: the image carries only libxml2-2.9.13-14.el9_8.4; `libxml2-python`
    is NOT installed and there is no python3 interpreter in the image at all (`command -v python3` finds
    nothing), so the Python bindings this finding describes cannot be loaded. No fix is available upstream
    (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
- cve_id: CVE-2026-18408
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-09-21 (#1173). PostgreSQL: Arbitrary code execution via untrusted
    data inclusion in pg_dump Unlike the server-side findings in this batch, `pg_dump` IS present in the
    image — deliberately NOT dispositioned as not-applicable, because the claim would be about reachability
    rather than absence, and reachability is a property of code that can change silently. SPARC reaches
    Postgres through the `pg` gem and libpq and does not shell out to `pg_dump` on any request path, so
    the exposure requires an operator to run it against untrusted input. No fix is available upstream
    (not-fixed), so there is no remediation to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
  deviation:
    type: risk_adjustment
    risk_status: deviation-approved
    approved_by: '@clem-field'
    approved_in: risk-sentinel/sparc#1173
    approved_at: '2026-09-22T00:33:13Z'
    approval_mechanism: admin-merge-bypass
    adjusted_severity: LOW
    adjustment_rationale: HIGH is the grading of the vulnerability in a deployment that RUNS the affected
      program against untrusted input. The mitigating factors below are properties of THIS image, measured
      rather than argued. No upstream fix exists, so remediation is not available and the residual is
      what is being accepted.
    mitigating_factors:
    - description: 'no PostgreSQL server in the image: postgres, postmaster, pg_ctl and initdb are all
        absent (command -v)'
    - description: SPARC reaches its database through the `pg` gem and libpq against a SEPARATE Postgres
        container; no application code path shells out to pg_dump
    - description: pg_dump acts on a database an OPERATOR points it at, so exploitation requires an operator
        to run it against a hostile server rather than any request reaching it
    kev_checked: '2026-09-21'
    kev_listed: false
- cve_id: CVE-2026-19385
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-09-21 (#1173). PostgreSQL pg_dump: Arbitrary code execution via crafted
    transform lists Unlike the server-side findings in this batch, `pg_dump` IS present in the image —
    deliberately NOT dispositioned as not-applicable, because the claim would be about reachability rather
    than absence, and reachability is a property of code that can change silently. SPARC reaches Postgres
    through the `pg` gem and libpq and does not shell out to `pg_dump` on any request path, so the exposure
    requires an operator to run it against untrusted input. No fix is available upstream (not-fixed),
    so there is no remediation to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
  deviation:
    type: risk_adjustment
    risk_status: deviation-approved
    approved_by: '@clem-field'
    approved_in: risk-sentinel/sparc#1173
    approved_at: '2026-09-22T00:33:13Z'
    approval_mechanism: admin-merge-bypass
    adjusted_severity: LOW
    adjustment_rationale: HIGH is the grading of the vulnerability in a deployment that RUNS the affected
      program against untrusted input. The mitigating factors below are properties of THIS image, measured
      rather than argued. No upstream fix exists, so remediation is not available and the residual is
      what is being accepted.
    mitigating_factors:
    - description: 'no PostgreSQL server in the image: postgres, postmaster, pg_ctl and initdb are all
        absent (command -v)'
    - description: SPARC reaches its database through the `pg` gem and libpq; no application code path
        shells out to pg_dump
    - description: the transform list is supplied on the pg_dump command line, so it is operator-controlled
        input and not attacker-controlled request data
    kev_checked: '2026-09-21'
    kev_listed: false
- cve_id: CVE-2026-6464
  package: postgresql/postgresql-private-libs
  installed_version: 13.23-5.el9_8
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-09-21 (#1173). PostgreSQL psql: Arbitrary command execution via untrusted
    data in COPY FROM STDIN Unlike the server-side findings in this batch, `psql` IS present in the image
    — deliberately NOT dispositioned as not-applicable, because the claim would be about reachability
    rather than absence, and reachability is a property of code that can change silently. SPARC reaches
    Postgres through the `pg` gem and libpq and does not shell out to `psql` on any request path, so the
    exposure requires an operator to run it against untrusted input. No fix is available upstream (not-fixed),
    so there is no remediation to apply. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
  deviation:
    type: risk_adjustment
    risk_status: deviation-approved
    approved_by: '@clem-field'
    approved_in: risk-sentinel/sparc#1173
    approved_at: '2026-09-22T00:33:13Z'
    approval_mechanism: admin-merge-bypass
    adjusted_severity: LOW
    adjustment_rationale: HIGH is the grading of the vulnerability in a deployment that RUNS the affected
      program against untrusted input. The mitigating factors below are properties of THIS image, measured
      rather than argued. No upstream fix exists, so remediation is not available and the residual is
      what is being accepted.
    mitigating_factors:
    - description: 'no PostgreSQL server in the image: postgres, postmaster, pg_ctl and initdb are all
        absent (command -v)'
    - description: SPARC reaches its database through the `pg` gem and libpq; no application code path
        invokes psql
    - description: COPY FROM STDIN requires an operator to run psql interactively against a hostile server;
        no request path reaches it
    kev_checked: '2026-09-21'
    kev_listed: false
- cve_id: CVE-2026-86140
  package: libxml2
  installed_version: 2.9.13-14.el9_8.4
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-09-21 (#1173). libxml2: Arbitrary code execution via stack-based buffer
    overflow in xmlSnprintfElements The system library IS present (/usr/lib64/libxml2.so.2), so this is
    not dispositioned as absent. It is however not on SPARC''s XML path: nokogiri ships as the precompiled
    `aarch64-linux-gnu` native gem and statically bundles its own libxml2 — `ldd` on nokogiri.so shows
    NO dynamic libxml2 link — so application XML parsing does not enter this copy. Recorded as accepted
    risk rather than not-applicable because that is a statement about reachability, which can change.
    No fix is available upstream (not-fixed).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
  deviation:
    type: risk_adjustment
    risk_status: deviation-approved
    approved_by: '@clem-field'
    approved_in: risk-sentinel/sparc#1173
    approved_at: '2026-09-22T00:33:13Z'
    approval_mechanism: admin-merge-bypass
    adjusted_severity: LOW
    adjustment_rationale: HIGH is the grading of the vulnerability in a deployment that RUNS the affected
      program against untrusted input. The mitigating factors below are properties of THIS image, measured
      rather than argued. No upstream fix exists, so remediation is not available and the residual is
      what is being accepted.
    mitigating_factors:
    - description: no package in the image requires libxml2 (rpm -q --whatrequires libxml2 returns nothing)
        — it is a base-image leftover with no consumer
    - description: nokogiri ships as the precompiled aarch64-linux-gnu native gem and statically bundles
        its OWN libxml2; ldd on nokogiri.so shows no dynamic libxml2 linkage, so application XML parsing
        never enters this copy
    - description: libpq does not link libxml2 either (ldd), so the database path does not reach it
    kev_checked: '2026-09-21'
    kev_listed: false
- cve_id: CVE-2026-86145
  package: pcre2/pcre2-syntax
  installed_version: 10.40-6.el9
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-09-21 (#1173). PCRE2: Out-of-bounds write allows arbitrary code execution
    via crafted regular expressions pcre2-10.40-6.el9 and pcre2-syntax are present and used by system
    tooling in the image. Not dispositioned as absent, because they are not absent. No fix is available
    upstream (not-fixed), so there is no remediation to apply and the finding is accepted for review.
    Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
  deviation:
    type: risk_adjustment
    risk_status: deviation-approved
    approved_by: '@clem-field'
    approved_in: risk-sentinel/sparc#1173
    approved_at: '2026-09-22T00:33:13Z'
    approval_mechanism: admin-merge-bypass
    adjusted_severity: LOW
    adjustment_rationale: HIGH is the grading of the vulnerability in a deployment that RUNS the affected
      program against untrusted input. The mitigating factors below are properties of THIS image, measured
      rather than argued. No upstream fix exists, so remediation is not available and the residual is
      what is being accepted.
    mitigating_factors:
    - description: Ruby does not link pcre2 — it uses Onigmo — verified by ldd on the ruby binary, so
        no application regular expression is evaluated by this library
    - description: the only package requiring pcre2 is libselinux, which does not process untrusted input
        at runtime
    - description: no attacker-controlled pattern reaches pcre2 on any request path
    kev_checked: '2026-09-21'
    kev_listed: false
- cve_id: CVE-2026-89161
  package: pcre2/pcre2-syntax
  installed_version: 10.40-6.el9
  fixed_version: none available upstream (not-fixed)
  severity: HIGH
  disposition: deferred
  rationale: 'POA&M / DEFERRED 2026-09-21 (#1173). PCRE2: Memory corruption vulnerability in pcre2_jit_match
    pcre2-10.40-6.el9 and pcre2-syntax are present and used by system tooling in the image. Not dispositioned
    as absent, because they are not absent. No fix is available upstream (not-fixed), so there is no remediation
    to apply and the finding is accepted for review. Scanned the IMAGE, not an SBOM (#862).'
  nist_control: si-2
  reviewed_by: '@clem-field'
  discovery_date: '2026-09-21'
  next_review_date: '2026-10-19'
  deviation:
    type: risk_adjustment
    risk_status: deviation-approved
    approved_by: '@clem-field'
    approved_in: risk-sentinel/sparc#1173
    approved_at: '2026-09-22T00:33:13Z'
    approval_mechanism: admin-merge-bypass
    adjusted_severity: LOW
    adjustment_rationale: HIGH is the grading of the vulnerability in a deployment that RUNS the affected
      program against untrusted input. The mitigating factors below are properties of THIS image, measured
      rather than argued. No upstream fix exists, so remediation is not available and the residual is
      what is being accepted.
    mitigating_factors:
    - description: Ruby does not link pcre2 — it uses Onigmo — verified by ldd on the ruby binary, so
        no application regular expression is evaluated by this library
    - description: the only package requiring pcre2 is libselinux, which does not process untrusted input
        at runtime
    - description: pcre2_jit_match is reached only through a pcre2 caller, and the image has none that
        sees untrusted input
    kev_checked: '2026-09-21'
    kev_listed: false
