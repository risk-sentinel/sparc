<%# The table header's select-all. Same control as the card grid's, without the
    visible label the header column already supplies. %>
<input type="checkbox" data-bulk-select-target="selectAll"
       data-action="bulk-select#toggleAll" aria-label="Select all">
