<div class="card">
  <div class="card-body">
  <h2 style="margin-bottom: 0.5rem;">Edit Control: <%= @catalog_control.display_id %></h2>
  <p class="text-body-secondary mb-4">
    Family: <%= link_to "#{@control_family.code} — #{@control_family.name}", @control_family, class: "text-primary" %>
  </p>
  <%= render "form", control_family: @control_family, catalog_control: @catalog_control %>
  </div>
</div>
